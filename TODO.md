# TODO — 自举开发(self-development)能力评估与推进计划

> 目标:cagent 开发的 agent 能够安全、自主地继续开发 cagent 自己。
> 评估日期:2026-08-16。评估基于当前代码基线(main @ dd8435a + 未提交的 runtime/tui 改动)。

## 评估结论

**基础闭环已跑通,但"让 agent 自主开发自己"目前处于 ~38% 加权完成度,且安全性不足,不建议开启无监督自主迭代。**

| 维度 | 权重 | 当前 | 说明 |
|---|---|---|---|
| 读-改-建-测闭环 | 30% | 70% | 已实际跑通,效率受限(串行 tool-call) |
| 安全/审批/回滚 | 25% | 10% | 最大瓶颈,零实现 |
| 规划与执行跟踪 | 15% | 10% | WAIT_USER 定义了但未用 |
| 记忆与状态恢复 | 15% | 30% | session 强,项目记忆无 |
| 工具完备性 | 10% | 40% | git/结构化测试缺失 |
| 自诊断 | 5% | 0% | 无可观测性 |
| **加权总完成度** | **100%** | **≈38%** | 分水岭在审批机制 |

## 已具备(自举闭环骨架)

- [x] **代码理解**:`read`/`grep` + `bash`(可调 rg/find/ctags),6 工具全部注册
- [x] **代码修改**:`write`(全量)/`edit`(精确替换)
- [x] **构建/测试**:经 `bash` 调 `cmake`/`ninja`/`ctest`(27 测试双构建)
- [x] **多步迭代**:状态机 loop(模型→工具→结果),挂起不占线程(`src/agent/loop.c`)
- [x] **上下文管理**:LLM compaction + 256KiB 摘要上限 + fallback
- [x] **会话持久化**:JSONL append-only + `-r` resume + compaction 事务恢复
- [x] **并发子任务**:`subagent` 并行数组 + 取消传播,100 Agent=1 线程
- [x] **部分自愈**:OOM 安全插入、request_id 精确取消、会话恢复

## 关键缺口(按自举重要性排序)

### P0 — 审批与安全边界(完成度 ≈10%,最致命)
- [ ] `tool.h:62` 的 `flags` 启用危险分级;当前 6 个工具全部 `TOOL_FLAG_NONE`
- [ ] `bash`/`write`/`edit` 危险操作审批流:挂起→TUI 展示 diff 预览→用户批准/拒绝
- [ ] 实现 `AGENT_WAIT_USER`(`agent.h:39`,已定义但 loop 从未使用)状态机路径——agent 目前无法主动提问/请示
- [ ] diff 预览(approve 前展示变更内容)
- [ ] 参考:Pi plan-mode 的 `PlanSpec`/`ApprovalRecord`/`ExecutionGrant` 分离设计与 extension-only 安全边界(见 `~/.config/pi/AGENTS.md` 评审结论)

### P0 — git 集成与回滚(完成度 ≈0%)
- [ ] 专用工具 `git_diff`:结构化展示变更(目前只能 bash 手写,agent 拿原始文本)
- [ ] 专用工具 `git_commit`:原子提交、规范化 message
- [ ] 专用工具 `git_revert`:构建失败自动回退到基线
- [ ] "改坏→自动 revert"机制(自举的前提是失败可逆)

### P1 — 任务规划与执行跟踪(完成度 ≈0%)
- [ ] plan/todo 机制:多步任务(调研→计划→改码→构建→测试→提交)分阶段执行
- [ ] 每步验收条件 + 失败重试策略
- [ ] 防止长任务漂移(模型单次输出无法承载长任务状态)

### P1 — 跨会话项目记忆(完成度 ≈20%)
- [ ] 将 `PROGRESS.md` 手工约定升级为 session 持久化的结构化决策记录(自动化)
- [ ] resume 时自动注入项目记忆("为什么这么设计、上次改到哪")
- [ ] 决策/拒绝/错误教训的跨会话检索(参考 context-mode 的 26 类事件自动记忆)

### P2 — 测试结果结构化理解(完成度 ≈40%)
- [ ] 专用测试工具返回结构化失败列表(而非解析 ctest 文本,防误读)
- [ ] "构建失败→定位到改动文件→修正→重跑"闭环支持

### P2 — 并行 tool-call(完成度 ≈40%)
- [ ] 同一 assistant 消息多 tool-call 并行调度与冲突控制(当前有意串行,自举"一次改 N 文件"被放大为 N 轮往返)
- [ ] batch edit 语义

### P3 — 自我诊断与 profile(完成度 ≈0%)
- [ ] 运行时指标暴露(内存/并发/延迟)给 agent 的接口
- [ ] `cagent_bench` 自动化调用与结果解读(优化自己的前提是能测量自己)

## 推进顺序(自举 MVP 路径)

1. **P0 审批层**:危险分级 → 审批挂起 → TUI diff 预览 → `AGENT_WAIT_USER` 状态机。预计将总完成度 38% → ~55%。
2. **P0 git 三件套**:`git_diff`/`git_commit`/`git_revert`,构建失败自动回退。
3. **P1 todo/plan**:Agent 内部任务列表 + 每步验收 + 重试策略。
4. **P1 项目记忆自动化**:PROGRESS.md 升级为结构化持久化决策记录。
5. **P2/P3**:并行 tool-call、结构化测试工具、运行时可观测性。

## 现有架构优势(自举的杠杆点)

- **静态工具注册 + 纯 C**:扩展 = 新增 `.c/.h` + 一行注册后重编译。agent "给自己加工具" 是成本最低的扩展路径——审批层完成后应优先验证这条路径。
- **无插件系统**:保持 minimal core / extension-first 哲学,不引入运行时动态加载复杂度。
- **Event Loop 独占可变状态**:从架构上消除锁,agent 间共享模型/HTTP 无竞态,自举时并发子任务安全。
